import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF1E1E1E);
  static const Color surface = Color(0xFF2C2C3E);
  //static const Color primary = Color(0xFF00D285); // Vibrant teal/green from image
  static const Color primary = Color.fromARGB(
    255,
    29,
    240,
    10,
  ); // Vibrant teal/green from image
  static const Color textBody = Color(0xFFB0B0C3);
  static const Color textHeading = Colors.white;
  static const Color accentPurple = Color(
    0xFF4A4A6A,
  ); // Background for menu tiles
}
