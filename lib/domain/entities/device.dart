class Device {
  final String id;
  final String name;
  final bool isConnected;
  final String iconAsset;

  const Device({
    required this.id,
    required this.name,
    required this.isConnected,
    required this.iconAsset,
  });
}
